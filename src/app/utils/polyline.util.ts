// STUB: maps integration is blocked on Barak. When a provider is chosen,
// dev replaces this with a real polyline decoder.
// React reference: projects/dispatch-system/app/src/lib/polyline.ts

export function decodePolyline(_encoded: string): Array<[number, number]> {
  return [];
}
